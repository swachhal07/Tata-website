# Logs
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

# Build artefacts
node_modules/
dist/
dist-ssr/

# Editors
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
*.suo
*.sw?

# Vercel CLI link metadata (per-machine)
.vercel/

# Env files (real secrets) — keep .example checked in
.env
.env.*
!.env.example

# Backend runtime data — local-only, never push
backend/data/
backend/uploads/
